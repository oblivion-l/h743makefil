# h743makefil

The STM32H743 project files live under `firmware/`.

From the repository root, the top-level `Makefile` forwards the common build
targets into `firmware/`, so commands such as `make`, `make clean`, and
`make flash` still work from here.
